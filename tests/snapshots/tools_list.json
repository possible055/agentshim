{
  "cacheScope": "private",
  "resultType": "complete",
  "tools": [
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Read a local repository or Codex extension source file as numbered text lines. Absolute paths may address configured Codex skill and plugin directories.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "encoding": {
            "description": "Optional WHATWG encoding label. A BOM takes precedence; otherwise valid UTF-8 is used when omitted.",
            "type": "string"
          },
          "line_count": {
            "description": "Maximum number of lines to return.",
            "maximum": 2000,
            "minimum": 1,
            "type": "integer"
          },
          "path": {
            "description": "Platform-native repository path or absolute path under a configured Codex skill or plugin directory.",
            "minLength": 1,
            "type": "string"
          },
          "start_line": {
            "default": 1,
            "description": "One-based first line to return.",
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "name": "read"
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Search local repository or Codex extension contents using Rust regex or fixed strings. Results are deterministic and expose structured numeric pagination.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "case": {
            "default": "smart",
            "enum": [
              "smart",
              "sensitive",
              "insensitive"
            ],
            "type": "string"
          },
          "context_lines": {
            "default": 0,
            "maximum": 20,
            "minimum": 0,
            "type": "integer"
          },
          "fixed_strings": {
            "default": false,
            "type": "boolean"
          },
          "glob": {
            "description": "Optional case-sensitive glob relative to the repository or requested Codex extension path.",
            "type": "string"
          },
          "limit": {
            "default": 200,
            "maximum": 1000,
            "minimum": 1,
            "type": "integer"
          },
          "mode": {
            "default": "content",
            "enum": [
              "content",
              "files",
              "count"
            ],
            "type": "string"
          },
          "offset": {
            "default": 0,
            "minimum": 0,
            "type": "integer"
          },
          "path": {
            "description": "Optional platform-native repository path or absolute path under a configured Codex skill or plugin directory.",
            "type": "string"
          },
          "pattern": {
            "description": "Rust regex by default, or a literal string when fixed_strings is true.",
            "type": "string"
          }
        },
        "required": [
          "pattern"
        ],
        "type": "object"
      },
      "name": "grep"
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Find local repository or Codex extension file paths using a glob pattern. Results use native absolute paths and expose structured numeric pagination.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "include_ignored": {
            "default": false,
            "type": "boolean"
          },
          "limit": {
            "default": 200,
            "maximum": 1000,
            "minimum": 1,
            "type": "integer"
          },
          "offset": {
            "default": 0,
            "minimum": 0,
            "type": "integer"
          },
          "path": {
            "default": ".",
            "description": "Platform-native repository directory or absolute directory under a configured Codex skill or plugin root.",
            "type": "string"
          },
          "pattern": {
            "description": "Case-sensitive glob relative to the repository or requested Codex extension directory.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "pattern"
        ],
        "type": "object"
      },
      "name": "glob"
    },
    {
      "annotations": {
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": true,
        "readOnlyHint": false
      },
      "description": "Run one permitted local program directly with literal arguments and no shell. There is no shell, so pipes, redirection, globbing, and variable expansion do not happen. Arguments are passed literally — do not add quoting. Prefer this when arguments contain characters a shell would mangle, such as Windows paths, regexes, and JSON. If the program is not permitted, this returns a not_permitted error — use bash instead; do not work around it. This is an open-world, destructive operation and may require approval.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "args": {
            "default": [],
            "description": "Literal argv elements; do not add shell quoting.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "cwd": {
            "description": "Optional platform-native absolute or repository-root-relative working directory; defaults to the repository root.",
            "type": "string"
          },
          "env": {
            "additionalProperties": {
              "type": "string"
            },
            "default": {},
            "description": "Environment variables to override.",
            "type": "object"
          },
          "program": {
            "description": "One program name or executable path, never a command string.",
            "minLength": 1,
            "type": "string"
          },
          "stdin": {
            "description": "Optional UTF-8 standard input, closed after writing.",
            "maxLength": 1048576,
            "type": [
              "string",
              "null"
            ]
          },
          "timeout_ms": {
            "default": 120000,
            "maximum": 600000,
            "minimum": 1,
            "type": "integer"
          },
          "unset_env": {
            "default": [],
            "description": "Inherited environment variables to remove.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "program"
        ],
        "type": "object"
      },
      "name": "run_program"
    },
    {
      "annotations": {
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": true,
        "readOnlyHint": false
      },
      "description": "Run a POSIX bash command line and return merged stdout and stderr with the exit code. Write POSIX bash, never PowerShell, on every platform. The command runs non-interactively with no TTY and stdin closed, so pass flags such as -y or --no-edit instead of expecting a prompt. A non-zero exit code is a normal result, not a tool error. Both output streams are merged into one pipe, so lines appear in pipe-write order and cannot be attributed to a stream; a program that buffers stdout but not stderr can still interleave them differently from what a terminal would show. Output above the byte budget is truncated in the middle: redirect to a file and page it with read when you need all of it. The default timeout is 120000 ms and the maximum is 600000 ms; for work that needs longer, set detach with a log_path and read that file instead of waiting. Do not issue state-changing commands against the same working tree in parallel calls. This is an open-world, destructive operation and may require approval.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "command": {
            "description": "POSIX bash command line, run with --noprofile --norc -c.",
            "minLength": 1,
            "type": "string"
          },
          "cwd": {
            "description": "Optional platform-native absolute or repository-root-relative working directory; defaults to the repository root.",
            "type": "string"
          },
          "detach": {
            "default": false,
            "description": "Run the command past the end of this call, bound to the server's lifetime. Requires log_path and forbids timeout_ms; returns the pid and log path instead of output.",
            "type": "boolean"
          },
          "log_path": {
            "description": "Repository-relative or root-absolute file for the detached command's merged output, truncated at start. Its parent directory must already exist. Required when detach is true and rejected otherwise.",
            "type": "string"
          },
          "timeout_ms": {
            "default": 120000,
            "maximum": 600000,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "command"
        ],
        "type": "object"
      },
      "name": "bash"
    }
  ],
  "ttlMs": 300000
}
