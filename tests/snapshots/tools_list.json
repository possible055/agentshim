{
  "cacheScope": "private",
  "resultType": "complete",
  "tools": [
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Read a local repository or Codex extension file. Text files are returned as numbered lines; PDFs default to page-oriented Markdown and can be rendered as images. Absolute paths may address configured Codex skill and plugin directories.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "encoding": {
            "description": "Optional WHATWG encoding label. A BOM takes precedence; when omitted, UTF-8, Big5, and GBK/GB18030 are detected conservatively.",
            "type": "string"
          },
          "line_count": {
            "description": "Maximum number of lines to return.",
            "maximum": 2000,
            "minimum": 1,
            "type": "integer"
          },
          "pages": {
            "description": "PDF only: one-based page or continuous page range, such as \"3\" or \"1-5\". A range extending past the last page is clamped to it; a range starting past it is an error.",
            "pattern": "^[1-9][0-9]*(-[1-9][0-9]*)?$",
            "type": "string"
          },
          "path": {
            "description": "Platform-native repository path or absolute path under a configured Codex skill or plugin directory.",
            "minLength": 1,
            "type": "string"
          },
          "pdf_mode": {
            "default": "auto",
            "description": "PDF only: \"auto\" and \"text\" return page Markdown, \"image\" renders PNG content blocks. Without pages, text modes deliver the first 10 pages and image renders page 1, each with a continuation.",
            "enum": [
              "auto",
              "text",
              "image"
            ],
            "type": "string"
          },
          "pdf_source_id": {
            "description": "PDF only: opaque token from a previous response, replayed to prove the continuation targets the same source version.",
            "minLength": 1,
            "type": "string"
          },
          "pdf_text_offset": {
            "description": "PDF only: resume a single page's Markdown at this UTF-8 byte offset. Requires pages to name exactly one page, and a non-zero value requires pdf_source_id.",
            "minimum": 0,
            "type": "integer"
          },
          "start_line": {
            "default": 1,
            "description": "One-based first line to return.",
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "name": "read"
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Search local repository or Codex extension contents using Rust regex or fixed strings. Results are deterministic and expose structured numeric pagination.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "case": {
            "default": "smart",
            "description": "Case sensitivity. smart is case-sensitive when the pattern contains an uppercase letter, otherwise case-insensitive.",
            "enum": [
              "smart",
              "sensitive",
              "insensitive"
            ],
            "type": "string"
          },
          "context_lines": {
            "default": 0,
            "description": "Lines of context to show before and after each match.",
            "maximum": 20,
            "minimum": 0,
            "type": "integer"
          },
          "fixed_strings": {
            "default": false,
            "description": "Treat pattern as a literal string instead of a Rust regex.",
            "type": "boolean"
          },
          "glob": {
            "description": "Optional case-sensitive glob relative to the repository or requested Codex extension path.",
            "type": "string"
          },
          "limit": {
            "default": 200,
            "description": "Maximum matching entries to return.",
            "maximum": 1000,
            "minimum": 1,
            "type": "integer"
          },
          "mode": {
            "default": "content",
            "description": "content returns matching lines, files returns only paths with matches, count returns path:count summaries.",
            "enum": [
              "content",
              "files",
              "count"
            ],
            "type": "string"
          },
          "offset": {
            "default": 0,
            "description": "Skip this many matching entries before returning results, for pagination.",
            "minimum": 0,
            "type": "integer"
          },
          "path": {
            "description": "Optional platform-native repository path or absolute path under a configured Codex skill or plugin directory.",
            "type": "string"
          },
          "pattern": {
            "description": "Rust regex by default, or a literal string when fixed_strings is true.",
            "type": "string"
          }
        },
        "required": [
          "pattern"
        ],
        "type": "object"
      },
      "name": "grep"
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Find local repository or Codex extension paths using a glob pattern. Returns files by default; use type to find directories or any entry. Results use native absolute paths and expose structured numeric pagination.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "include_ignored": {
            "default": false,
            "description": "Include entries matched by gitignore; .git internals remain excluded.",
            "type": "boolean"
          },
          "limit": {
            "default": 200,
            "description": "Maximum paths to return.",
            "maximum": 1000,
            "minimum": 1,
            "type": "integer"
          },
          "offset": {
            "default": 0,
            "description": "Skip this many matching paths before returning results, for pagination.",
            "minimum": 0,
            "type": "integer"
          },
          "path": {
            "default": ".",
            "description": "Platform-native repository directory or absolute directory under a configured Codex skill or plugin root.",
            "type": "string"
          },
          "pattern": {
            "description": "Case-sensitive glob relative to the repository or requested Codex extension directory.",
            "minLength": 1,
            "type": "string"
          },
          "type": {
            "default": "file",
            "description": "Filesystem entry kind to return. file includes regular files and symbolic-link entries; directory returns real directories; any preserves both.",
            "enum": [
              "file",
              "directory",
              "any"
            ],
            "type": "string"
          }
        },
        "required": [
          "pattern"
        ],
        "type": "object"
      },
      "name": "glob"
    },
    {
      "annotations": {
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": true,
        "readOnlyHint": false
      },
      "description": "Run one local program directly with literal arguments and no shell. Use this by default whenever one executable is enough, including an interpreter whose script or command text is passed as one literal argument. Pipes, redirection, globbing, and variable expansion do not happen. Arguments are passed literally — do not add quoting. Prefer this when arguments contain characters a shell would mangle, such as Windows paths, regexes, and JSON. Use bash only when shell composition is required. Do not issue state-changing commands against the same working tree in parallel calls. Cleanup owns a Windows Job Object or the Unix process group it created; a Unix program that starts a new session can escape that group. This is not a sandbox. This is an open-world, destructive operation and may require approval.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "args": {
            "default": [],
            "description": "Literal argv elements; do not add shell quoting.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "cwd": {
            "description": "Optional platform-native absolute or repository-root-relative working directory; defaults to the repository root.",
            "type": "string"
          },
          "env": {
            "additionalProperties": {
              "type": "string"
            },
            "default": {},
            "description": "Environment variables to override.",
            "type": "object"
          },
          "program": {
            "description": "One program name or executable path, never a command string.",
            "minLength": 1,
            "type": "string"
          },
          "stdin": {
            "description": "Optional UTF-8 standard input, closed after writing.",
            "maxLength": 1048576,
            "type": [
              "string",
              "null"
            ]
          },
          "timeout_ms": {
            "default": 120000,
            "description": "Execution timeout in milliseconds. On timeout the owned process containment is terminated and a Timeout error is returned.",
            "maximum": 590000,
            "minimum": 1,
            "type": "integer"
          },
          "unset_env": {
            "default": [],
            "description": "Inherited environment variables to remove.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "program"
        ],
        "type": "object"
      },
      "name": "run_program"
    },
    {
      "annotations": {
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": true,
        "readOnlyHint": false
      },
      "description": "Run a POSIX bash command line and return merged stdout and stderr with the exit code. Write POSIX bash, never PowerShell, on every platform. The command runs non-interactively with no TTY and stdin closed, so pass flags such as -y or --no-edit instead of expecting a prompt. A non-zero exit code is a normal result, not a tool error. Both output streams are merged into one pipe, so lines appear in pipe-write order and cannot be attributed to a stream; a program that buffers stdout but not stderr can still interleave them differently from what a terminal would show. Output above the byte budget is truncated in the middle: redirect to a file and page it with read when you need all of it. The default timeout is 120000 ms and the maximum is 590000 ms; for work that needs longer, set detach with a log_path and read that file instead of waiting. On Windows, prefer run_program for one native program with literal arguments. When Bash composition must pass slash-style switches such as /E or /C to a native program, set msys_argument_conversion to disabled. This setting applies to the whole Bash command; codexshim does not inspect subcommands or retry failures. Do not issue state-changing commands against the same working tree in parallel calls. Cleanup owns a Windows Job Object or the Unix process group it created; a Unix program that starts a new session can escape that group. This is not a sandbox. This is an open-world, destructive operation and may require approval.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "command": {
            "description": "POSIX bash command line, run with --noprofile --norc -c.",
            "minLength": 1,
            "type": "string"
          },
          "cwd": {
            "description": "Optional platform-native absolute or repository-root-relative working directory; defaults to the repository root.",
            "type": "string"
          },
          "detach": {
            "default": false,
            "description": "Run the command past the end of this call under server-owned lifecycle tracking. Windows owns a Job Object; Unix owns the created process group, which a program can escape by starting a new session. Requires log_path and forbids timeout_ms; returns the pid and log path instead of output.",
            "type": "boolean"
          },
          "log_path": {
            "description": "Repository-relative or root-absolute file for the detached command's merged output, truncated at start. Its parent directory must already exist. Required when detach is true and rejected otherwise.",
            "type": "string"
          },
          "msys_argument_conversion": {
            "default": "default",
            "description": "Windows Git Bash only. default leaves inherited MSYS settings and automatic argv path conversion unchanged. disabled sets MSYS2_ARG_CONV_EXCL=* for the whole Bash command so native child processes receive slash-prefixed arguments unchanged. It has no effect on macOS or Linux. Prefer run_program for a single native program.",
            "enum": [
              "default",
              "disabled"
            ],
            "type": "string"
          },
          "timeout_ms": {
            "default": 120000,
            "description": "Execution timeout in milliseconds. On timeout the owned process group is terminated and a Timeout error is returned. Forbidden when detach is true.",
            "maximum": 590000,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "command"
        ],
        "type": "object"
      },
      "name": "bash"
    }
  ],
  "ttlMs": 300000
}
