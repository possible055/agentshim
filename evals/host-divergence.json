{
  "bash": {
    "mcp": {
      "owner": "server-detached-tree",
      "variants": [
        {
          "fields": ["command", "cwd", "detach", "msys_argument_conversion", "timeout_ms"],
          "required": ["command"]
        },
        {
          "fields": ["command", "cwd", "detach", "log_path", "msys_argument_conversion", "timeout_ms"],
          "required": ["command", "detach", "log_path"]
        },
        {
          "fields": ["action", "job_id"],
          "required": ["action", "job_id"]
        }
      ]
    },
    "dsh": {
      "owner": "dsh-jobs",
      "fields": ["command", "description", "msys_argument_conversion", "run_in_background", "timeoutMs", "workdir"],
      "required": ["command", "description"]
    },
    "intentionalDifferences": [
      "MCP exposes separate foreground, detached, and terminate variants; DSH exposes run_in_background and delegates lifecycle to ctx.jobs.",
      "DSH requires a model-facing description and uses workdir/timeoutMs names; MCP uses cwd/timeout_ms."
    ]
  },
  "bash_status": {
    "mcp": {
      "owner": "server-detached-tree",
      "fields": ["cursor", "job_id", "max_bytes", "tail_bytes", "wait_ms"],
      "required": ["job_id"]
    },
    "dsh": {
      "owner": "dsh-jobs",
      "fields": ["job_id"],
      "required": ["job_id"]
    },
    "intentionalDifferences": [
      "MCP reads a bounded retained log tail or cursor page and may wait briefly for a new snapshot; DSH returns a non-consuming ctx.jobs lifecycle snapshot. Neither wait changes the job deadline."
    ]
  }
}
