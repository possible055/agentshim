[project]
name = "codexshim"
version = "0"
description = "Local Python tooling for codexshim (pre-commit hooks)"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pre-commit>=4.5.0",
]

[tool.uv]
package = false
